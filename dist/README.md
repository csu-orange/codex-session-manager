# Distribution Notes

This directory is for built release artifacts that are convenient for manual testing or temporary sharing.

Current contents:

- `windows/codex-session-manager.exe`: portable Windows executable built from the Rust + Tauri app

For normal source control workflows, the authoritative source code lives in:

- `public/`
- `src-tauri/`

